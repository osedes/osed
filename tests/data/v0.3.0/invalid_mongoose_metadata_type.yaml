osed: "0.3.0"

entities:
  - User

User:
  id: systemId
  tags:
    mongoose:type: true  # Invalid, should be string
    mongoose:items: string
    mongoose:required: "yes"  # Invalid, should be boolean
